---
phase: 03-web-rewrite
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - editor-tx/src/components/NavBar/NavBar.tsx
  - editor-tx/src/components/NavBar/NavBar.module.css
  - editor-tx/src/components/NavBar/index.ts
  - editor-tx/src/components/Footer/Footer.tsx
  - editor-tx/src/components/Footer/Footer.module.css
  - editor-tx/src/components/Footer/index.ts
  - editor-tx/src/components/SyncIndicator/SyncIndicator.tsx
  - editor-tx/src/components/SyncIndicator/SyncIndicator.module.css
  - editor-tx/src/components/SyncIndicator/index.ts
  - editor-tx/src/components/BankSelector/BankSelector.tsx
  - editor-tx/src/components/BankSelector/BankSelector.module.css
  - editor-tx/src/components/BankSelector/index.ts
  - editor-tx/src/pages/Layout/Layout.tsx
  - editor-tx/src/pages/Layout/Layout.module.css
  - editor-tx/src/pages/Layout/index.ts
  - editor-tx/src/pages/Dashboard/Dashboard.tsx
  - editor-tx/src/pages/Dashboard/Dashboard.module.css
  - editor-tx/src/pages/Dashboard/index.ts
  - editor-tx/src/components/KeyCard/KeyCard.tsx
  - editor-tx/src/components/KeyCard/KeyCard.module.css
  - editor-tx/src/components/KeyCard/index.ts
  - editor-tx/src/components/SelectCard/SelectCard.tsx
  - editor-tx/src/components/SelectCard/SelectCard.module.css
  - editor-tx/src/components/SelectCard/index.ts
  - editor-tx/src/components/SliderCard/SliderCard.tsx
  - editor-tx/src/components/SliderCard/SliderCard.module.css
  - editor-tx/src/components/SliderCard/index.ts
  - editor-tx/src/components/ToggleCard/ToggleCard.tsx
  - editor-tx/src/components/ToggleCard/ToggleCard.module.css
  - editor-tx/src/components/ToggleCard/index.ts
  - editor-tx/src/components/NumberCard/NumberCard.tsx
  - editor-tx/src/components/NumberCard/NumberCard.module.css
  - editor-tx/src/components/NumberCard/index.ts
  - editor-tx/src/components/CcCard/CcCard.tsx
  - editor-tx/src/components/CcCard/CcCard.module.css
  - editor-tx/src/components/CcCard/index.ts
autonomous: true
requirements:
  - WEBFEAT-06

must_haves:
  truths:
    - "NavBar shows Editor/Update/Manual navigation with tooltips and sync status indicator"
    - "Dashboard page has 4 tabs: Keyboard, Custom Scales, CC Mapping, Settings"
    - "All config cards use design system primitives and update via useConfig().updateParam()"
    - "Layout is responsive: single column on mobile, two-column grid on tablet/desktop"
    - "Content area max-width is 960px, centered, with correct padding per UI-SPEC"
  artifacts:
    - path: "editor-tx/src/pages/Layout/Layout.tsx"
      provides: "Root layout with NavBar + Outlet + Footer"
      contains: "Outlet"
    - path: "editor-tx/src/pages/Dashboard/Dashboard.tsx"
      provides: "Dashboard page with 4 tabbed sections"
      contains: "Tabs"
    - path: "editor-tx/src/components/NavBar/NavBar.tsx"
      provides: "Navigation bar with sync indicator"
      contains: "useConnection"
    - path: "editor-tx/src/components/SelectCard/SelectCard.tsx"
      provides: "Config card wrapping design system Select"
      contains: "useConfig"
  key_links:
    - from: "editor-tx/src/components/KeyCard/KeyCard.tsx"
      to: "editor-tx/src/hooks/useConfig.ts"
      via: "useConfig() for config reads and updateParam calls"
      pattern: "useConfig"
    - from: "editor-tx/src/pages/Dashboard/Dashboard.tsx"
      to: "editor-tx/src/design-system/Tabs"
      via: "Tabs component for section switching"
      pattern: "import.*from.*@/design-system"
    - from: "editor-tx/src/pages/Layout/Layout.tsx"
      to: "editor-tx/src/components/NavBar"
      via: "NavBar rendered in layout"
      pattern: "import.*NavBar"
---

<objective>
Build the app shell (NavBar, Footer, Layout) and rewrite the Dashboard page with all 4 tab sections (Keyboard, Custom Scales, CC Mapping, Settings) using the new design system and contexts.

Purpose: The Dashboard is the primary page users interact with. This plan delivers the core UI (WEBFEAT-06) consuming the design system (Plan 02) and contexts (Plan 03).
Output: Complete layout shell and fully functional Dashboard page with all configuration tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-rewrite/03-CONTEXT.md
@.planning/phases/03-web-rewrite/03-UI-SPEC.md
@.planning/phases/03-web-rewrite/03-02-SUMMARY.md
@.planning/phases/03-web-rewrite/03-03-SUMMARY.md

<interfaces>
From editor-tx/src/design-system/index.ts (Plan 02):
```typescript
export { Button } from './Button'
export { Card } from './Card'
export { Select } from './Select'
export { Switch } from './Switch'
export { Slider } from './Slider'
export { Tabs, TabsList, TabsTrigger, TabsContent } from './Tabs'
export { Tooltip, TooltipTrigger, TooltipContent } from './Tooltip'
export { Skeleton } from './Skeleton'
```

From editor-tx/src/hooks/ (Plan 03):
```typescript
export function useConnection(): { input, output, isConnected, isDemo, connect, disconnect, setDemo }
export function useConfig(): { config, deviceConfig, selectedBank, isSynced, setBank, updateParam }
export function useToast(): { toast }
```

From editor-tx/src/types/config.ts:
```typescript
export interface T16Configuration { version: 200; global: {...}; banks: BankConfig[] }
```

From editor-tx/src/protocol/sysex.ts:
```typescript
export const DOMAIN = { GLOBAL: 0x00, BANK_KB: 0x01, BANK_CC: 0x02 } as const
export const FIELD_GLOBAL = { MODE: 0, SENSITIVITY: 1, BRIGHTNESS: 2, MIDI_TRS: 3, TRS_TYPE: 4, PASSTHROUGH: 5, MIDI_BLE: 6 } as const
export const FIELD_BANK = { CHANNEL: 0, SCALE: 1, OCTAVE: 2, NOTE: 3, VELOCITY_CURVE: 4, AFTERTOUCH_CURVE: 5, FLIP_X: 6, FLIP_Y: 7, KOALA_MODE: 8 } as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout shell (NavBar, Footer, SyncIndicator, RootLayout)</name>
  <files>
    editor-tx/src/components/NavBar/NavBar.tsx
    editor-tx/src/components/NavBar/NavBar.module.css
    editor-tx/src/components/NavBar/index.ts
    editor-tx/src/components/Footer/Footer.tsx
    editor-tx/src/components/Footer/Footer.module.css
    editor-tx/src/components/Footer/index.ts
    editor-tx/src/components/SyncIndicator/SyncIndicator.tsx
    editor-tx/src/components/SyncIndicator/SyncIndicator.module.css
    editor-tx/src/components/SyncIndicator/index.ts
    editor-tx/src/pages/Layout/Layout.tsx
    editor-tx/src/pages/Layout/Layout.module.css
    editor-tx/src/pages/Layout/index.ts
  </files>
  <read_first>
    editor-tx/src/components/NavBar.jsx
    editor-tx/src/components/Footer.jsx
    editor-tx/src/layouts/RootLayout.jsx
    .planning/phases/03-web-rewrite/03-UI-SPEC.md
  </read_first>
  <action>
1. **NavBar** (`editor-tx/src/components/NavBar/NavBar.tsx`):
- Sticky top, height var(--nav-height) (56px), --gray-800 background, white text
- Left: icon nav links for "Editor" (link to /), "Update" (link to /upload), "Manual" (link to /manual) -- use react-icons Material Design icons (MdTune, MdSystemUpdate, MdMenuBook). Each wrapped in Tooltip showing the nav label per UI-SPEC copywriting.
- Center: "T16 Editor" title text
- Right: SyncIndicator component + Connect/Disconnect button
- Use `useConnection()` for isConnected state, connect/disconnect actions
- Use `NavLink` from `react-router` for active state styling (active link gets --primary-800 bottom border)
- Touch targets minimum 44px per UI-SPEC
- Responsive: on mobile, title hidden, icons only

2. **SyncIndicator** (`editor-tx/src/components/SyncIndicator/SyncIndicator.tsx`):
- 8px filled dot showing connection/sync state per UI-SPEC:
  - Connected + synced: --color-success
  - Connected + unsynced: --color-warning
  - Disconnected: --color-error
- Uses `useConnection()` for isConnected and `useConfig()` for isSynced

3. **Footer** (`editor-tx/src/components/Footer/Footer.tsx`):
- Static footer, --gray-100 background
- Firmware version display (from useConfig if available)
- Simple copyright or brand text

4. **Layout** (`editor-tx/src/pages/Layout/Layout.tsx`):
- Root layout component for react-router Outlet pattern
- Structure: NavBar at top, main content area with `<Outlet />`, Footer at bottom
- Content area: max-width var(--content-max-width) (960px), margin 0 auto, horizontal padding var(--space-2xl) on desktop / var(--space-md) on mobile
- Min-height: 100vh with flex column layout
  </action>
  <verify>
    <automated>cd editor-tx && npx tsc --noEmit src/components/NavBar/NavBar.tsx src/components/Footer/Footer.tsx src/components/SyncIndicator/SyncIndicator.tsx src/pages/Layout/Layout.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NavBar renders 3 nav links with tooltips and sync indicator
    - NavBar uses useConnection() for connect/disconnect
    - SyncIndicator shows correct color based on connection + sync state
    - Footer shows firmware version
    - Layout uses Outlet from react-router and has max-width 960px content area
    - All files use CSS Modules with var(--token) references
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>App shell with NavBar, Footer, SyncIndicator, and root Layout ready for page content</done>
</task>

<task type="auto">
  <name>Task 2: Create config card components (SelectCard, SliderCard, ToggleCard, NumberCard, KeyCard, CcCard, BankSelector)</name>
  <files>
    editor-tx/src/components/BankSelector/BankSelector.tsx
    editor-tx/src/components/BankSelector/BankSelector.module.css
    editor-tx/src/components/BankSelector/index.ts
    editor-tx/src/components/SelectCard/SelectCard.tsx
    editor-tx/src/components/SelectCard/SelectCard.module.css
    editor-tx/src/components/SelectCard/index.ts
    editor-tx/src/components/SliderCard/SliderCard.tsx
    editor-tx/src/components/SliderCard/SliderCard.module.css
    editor-tx/src/components/SliderCard/index.ts
    editor-tx/src/components/ToggleCard/ToggleCard.tsx
    editor-tx/src/components/ToggleCard/ToggleCard.module.css
    editor-tx/src/components/ToggleCard/index.ts
    editor-tx/src/components/NumberCard/NumberCard.tsx
    editor-tx/src/components/NumberCard/NumberCard.module.css
    editor-tx/src/components/NumberCard/index.ts
    editor-tx/src/components/KeyCard/KeyCard.tsx
    editor-tx/src/components/KeyCard/KeyCard.module.css
    editor-tx/src/components/KeyCard/index.ts
    editor-tx/src/components/CcCard/CcCard.tsx
    editor-tx/src/components/CcCard/CcCard.module.css
    editor-tx/src/components/CcCard/index.ts
  </files>
  <read_first>
    editor-tx/src/components/SelectCard.jsx
    editor-tx/src/components/SliderCard.jsx
    editor-tx/src/components/ToggleCard.jsx
    editor-tx/src/components/NumberCard.jsx
    editor-tx/src/components/KeyCard.jsx
    editor-tx/src/components/CcCard.jsx
    editor-tx/src/components/BankSelector.jsx
  </read_first>
  <action>
Each card component wraps a design system primitive inside a Card with a label. They are the building blocks for Dashboard tab content. Study the existing JSX files to preserve exact functionality.

1. **BankSelector**: Renders 4 bank buttons (A, B, C, D). Uses `useConfig()` for selectedBank and setBank(). Active bank gets --primary-800 styling. Wraps Button components.

2. **SelectCard**: Props: `label: string`, `value: string`, `onValueChange: (v: string) => void`, `options: {value: string, label: string}[]`, `disabled?: boolean`. Wraps design-system Card + Select. Shows per-parameter sync dot (6px --color-warning dot next to label) when value differs from device state.

3. **SliderCard**: Props: `label: string`, `value: number`, `onValueChange: (v: number) => void`, `min: number`, `max: number`, `step?: number`, `disabled?: boolean`. Wraps Card + Slider. Shows current value as number next to slider.

4. **ToggleCard**: Props: `label: string`, `checked: boolean`, `onCheckedChange: (v: boolean) => void`, `disabled?: boolean`. Wraps Card + Switch.

5. **NumberCard**: Props: `label: string`, `value: number`, `onChange: (v: number) => void`, `min: number`, `max: number`, `disabled?: boolean`. Wraps Card with increment/decrement buttons and number display.

6. **KeyCard**: Renders a single key's configuration for the 4x4 keyboard grid. Shows key index, current note assignment. Used in the Keyboard tab.

7. **CcCard**: Renders a single CC mapping row. Props: `index: number`, `channel: number`, `ccId: number`, `onChannelChange`, `onIdChange`. Uses Select for channel and NumberCard-style input for CC ID. Used in CC Mapping tab.

All card components:
- Use CSS Modules with design tokens
- Use TypeScript interfaces for props (no PropTypes)
- Named exports with barrel index.ts
- Per-parameter sync dot where applicable (compare config vs deviceConfig from useConfig)
  </action>
  <verify>
    <automated>cd editor-tx && npx tsc --noEmit src/components/SelectCard/SelectCard.tsx src/components/SliderCard/SliderCard.tsx src/components/ToggleCard/ToggleCard.tsx src/components/BankSelector/BankSelector.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 7 card components created with .tsx + .module.css + index.ts
    - Each wraps design system primitives (Card, Select, Switch, Slider, Button)
    - TypeScript interfaces for all props
    - CSS Modules use var(--token) references
    - No PropTypes imports anywhere
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All config card components built with design system primitives, ready for Dashboard composition</done>
</task>

<task type="auto">
  <name>Task 3: Create Dashboard page with 4 tabbed sections</name>
  <files>
    editor-tx/src/pages/Dashboard/Dashboard.tsx
    editor-tx/src/pages/Dashboard/Dashboard.module.css
    editor-tx/src/pages/Dashboard/index.ts
  </files>
  <read_first>
    editor-tx/src/pages/Dashboard.jsx
    editor-tx/src/pages/Keyboard.jsx
    editor-tx/src/pages/Scales.jsx
    editor-tx/src/pages/ControlChange.jsx
    editor-tx/src/pages/Settings.jsx
  </read_first>
  <action>
Create the Dashboard page combining the current Dashboard + Keyboard + Scales + ControlChange + Settings pages into a single tabbed view.

Dashboard.tsx uses the design-system Tabs component with 4 tabs per UI-SPEC copywriting:

**Tab 1: "Keyboard"** (content from current Keyboard.jsx):
- BankSelector at top
- Grid of KeyCard components (4x4 grid on desktop, 2-column on mobile)
- SelectCard for mode, scale, root note
- SliderCard for velocity curve, aftertouch curve
- ToggleCard for flip X, flip Y, koala mode
- All cards call `useConfig().updateParam()` with appropriate DOMAIN.BANK_KB and FIELD_BANK constants

**Tab 2: "Custom Scales"** (content from current Scales.jsx):
- Custom scale editor for custom_scale1 and custom_scale2
- 16 note inputs per scale (match current EditableScaleDisplay behavior)
- Uses DOMAIN.GLOBAL for scale data updates

**Tab 3: "CC Mapping"** (content from current ControlChange.jsx):
- BankSelector at top
- List of 8 CcCard components per bank
- Each CcCard shows channel select and CC ID input
- Uses DOMAIN.BANK_CC and bank index for updates

**Tab 4: "Settings"** (content from current Settings.jsx):
- Global settings: sensitivity (SliderCard), brightness (SliderCard), MIDI TRS (ToggleCard), TRS type (SelectCard), passthrough (ToggleCard), BLE (ToggleCard)
- Demo mode toggle
- Calibration button (opens Dialog with destructive confirmation per UI-SPEC copywriting: "This will reset all key sensitivity data...")
- Factory reset button (opens AlertDialog per UI-SPEC: "This will erase all configuration...")
- All cards call updateParam with DOMAIN.GLOBAL and FIELD_GLOBAL constants

**Layout:**
- Tabs container below BankSelector (for Keyboard/CC tabs)
- Responsive grid: 1 column mobile, 2 columns tablet+
- Cards have --space-md gaps
- Tab panel padding: var(--space-lg)

**Empty state:** When not connected and not in demo mode, show empty state per UI-SPEC:
- Heading: "No Device Connected"
- Body: "Connect your T16 via USB to start editing. Make sure Chrome or Edge is open."
- Connect button
- Use Skeleton components for card placeholders
  </action>
  <verify>
    <automated>cd editor-tx && npx tsc --noEmit src/pages/Dashboard/Dashboard.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx renders Tabs with 4 sections: Keyboard, Custom Scales, CC Mapping, Settings
    - Keyboard tab has BankSelector, mode/scale/note selects, velocity/aftertouch sliders, flip/koala toggles
    - CC Mapping tab has BankSelector and 8 CcCards per bank
    - Settings tab has global config cards + calibration/factory reset dialogs
    - Empty state shown when disconnected (with UI-SPEC copywriting)
    - All param updates use correct DOMAIN and FIELD constants from @/protocol/sysex
    - Responsive grid layout with correct spacing
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Dashboard page complete with all 4 configuration tabs, consuming design system and contexts</done>
</task>

</tasks>

<verification>
- `cd editor-tx && npx tsc --noEmit` -- all new components and pages compile
- `grep -r "useConfig\|useConnection" editor-tx/src/pages/Dashboard/ editor-tx/src/components/NavBar/` -- contexts consumed
- `grep -r "import.*from.*@/design-system" editor-tx/src/pages/Dashboard/` -- design system used
- `grep -r "DOMAIN\|FIELD_GLOBAL\|FIELD_BANK" editor-tx/src/pages/Dashboard/` -- protocol constants used
</verification>

<success_criteria>
- Complete app shell (NavBar + Footer + Layout) with responsive design
- Dashboard page with 4 functional tabs matching current feature set
- All configuration parameters editable via design system cards
- Correct SysEx domain/field constants used for parameter updates
- Responsive layout matching UI-SPEC (single column mobile, two-column desktop)
- Empty state with UI-SPEC copywriting when disconnected
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-rewrite/03-04-SUMMARY.md`
</output>
