---
phase: 02-firmware-service-extraction
plan: 06
type: execute
wave: 4
depends_on: [02-05]
files_modified:
  - test/test_mode_manager/test_main.cpp
  - test/test_transport/test_main.cpp
  - test/test_slider/test_main.cpp
  - test/test_input_processor/test_main.cpp
  - test/native_stubs/Arduino.h
  - test/native_stubs/Arduino.cpp
autonomous: true
requirements: [TEST-01, FWBUG-02]

must_haves:
  truths:
    - "ModeManager state machine transitions are tested on native host"
    - "Transport abstraction dispatch is tested with mock transports"
    - "TouchSlider::SetPosition(uint8_t, uint8_t) correctly assigns lastPosition, verified by test"
    - "InputProcessor key-to-note mapping is tested on native host"
    - "All tests pass via pio test -e native"
  artifacts:
    - path: "test/test_mode_manager/test_main.cpp"
      provides: "ModeManager unit tests"
      contains: "TEST_ASSERT"
    - path: "test/test_transport/test_main.cpp"
      provides: "Transport abstraction tests"
      contains: "TEST_ASSERT"
    - path: "test/test_slider/test_main.cpp"
      provides: "TouchSlider SetPosition bug fix verification"
      contains: "TEST_ASSERT"
    - path: "test/test_input_processor/test_main.cpp"
      provides: "InputProcessor tests"
      contains: "TEST_ASSERT"
  key_links:
    - from: "test/test_mode_manager/test_main.cpp"
      to: "src/services/ModeManager.hpp"
      via: "includes and tests ModeManager"
      pattern: "#include.*ModeManager"
    - from: "test/test_transport/test_main.cpp"
      to: "src/Libs/MidiTransport.hpp"
      via: "includes and mocks MidiTransport"
      pattern: "#include.*MidiTransport"
    - from: "test/test_slider/test_main.cpp"
      to: "src/Libs/TouchSlider.hpp"
      via: "includes and tests SetPosition"
      pattern: "#include.*TouchSlider"
---

<objective>
Add unit tests for the newly extracted services: ModeManager transitions, transport abstraction dispatch, TouchSlider SetPosition bug fix verification, and InputProcessor key-to-note mapping. Tests run on the PlatformIO native environment (host machine, no ESP32 required).

Purpose: Per D-10 and TEST-01, test coverage targets service interfaces and state machines with mocked hardware dependencies. The slider test verifies the FWBUG-02 fix (SetPosition no-op). These tests validate that the extraction preserved correct behavior.

Output: Four test suites passing on native host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-firmware-service-extraction/02-CONTEXT.md
@.planning/phases/02-firmware-service-extraction/02-RESEARCH.md
@.planning/phases/02-firmware-service-extraction/02-05-SUMMARY.md

@src/services/ModeManager.hpp
@src/services/ModeManager.cpp
@src/services/InputProcessor.hpp
@src/Libs/MidiTransport.hpp
@src/Libs/TouchSlider.hpp
@src/Libs/TouchSlider.cpp
@test/native_stubs/Arduino.h
@test/native_stubs/Arduino.cpp
@test/test_config_manager/test_main.cpp
@platformio.ini

<interfaces>
From existing test infrastructure (Phase 1):
- Unity test framework (PlatformIO built-in)
- test/native_stubs/Arduino.h provides millis(), delay(), log_d(), Serial stubs
- Pattern: include .cpp files directly in test_main.cpp for PlatformIO native linking
- Run: `pio test -e native -f test_<name>`
- Full suite: `pio test -e native`

From src/services/ModeManager.hpp:
```cpp
namespace t16 {
class ModeManager {
    void setMode(Mode mode);
    Mode getMode() const;
    SliderMode getSliderMode() const;
    void cycleSliderMode();
    const SliderMode* getAllowedSliderModes(size_t& count) const;
};
}
```

From src/Libs/MidiTransport.hpp:
```cpp
namespace t16 {
class MidiTransport {
    virtual void sendNoteOn(uint8_t note, uint8_t velocity, uint8_t channel) = 0;
    virtual void sendNoteOff(uint8_t note, uint8_t velocity, uint8_t channel) = 0;
    // ... other virtual methods
};
}
```

From src/Libs/TouchSlider.hpp:
```cpp
class TouchSlider {
    void SetPosition(float position) { lastPosition = position; };
    void SetPosition(uint8_t intPosition, uint8_t numPositions);
    float GetPosition() { return lastPosition; };
    // lastPosition is a float member
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModeManager, transport abstraction, and slider SetPosition tests</name>
  <files>test/test_mode_manager/test_main.cpp, test/test_transport/test_main.cpp, test/test_slider/test_main.cpp, test/native_stubs/Arduino.h</files>
  <read_first>
    - test/test_config_manager/test_main.cpp (existing test pattern — how .cpp files are included, Unity macros used, test runner setup)
    - test/native_stubs/Arduino.h (current stubs — check if Mode/SliderMode enums need stub definitions or if Types.hpp can be included directly)
    - test/native_stubs/Arduino.cpp (current stub implementations)
    - src/services/ModeManager.hpp (class to test)
    - src/services/ModeManager.cpp (implementation to include)
    - src/Types.hpp (enum definitions needed by tests)
    - src/Libs/MidiTransport.hpp (interface to mock)
    - src/Libs/TouchSlider.hpp (class declaration — SetPosition overloads, lastPosition member)
    - src/Libs/TouchSlider.cpp (implementation — the fixed SetPosition(uint8_t, uint8_t) overload)
    - platformio.ini (native test environment configuration — check test_filter, build_flags, lib_ignore)
  </read_first>
  <behavior>
    ModeManager tests:
    - test_initial_mode_is_keyboard: getMode() == Mode::KEYBOARD after construction
    - test_initial_slider_mode_is_bend: getSliderMode() == SliderMode::BEND after construction
    - test_set_mode_strum: setMode(STRUM) -> getMode() returns STRUM, getSliderMode() returns STRUMMING (default for strum)
    - test_set_mode_xy_pad: setMode(XY_PAD) -> getSliderMode() returns SLEW
    - test_cycle_slider_keyboard: in KEYBOARD mode, cycleSliderMode 4 times cycles BEND->MOD->OCTAVE->BANK->BEND
    - test_cycle_slider_strum: in STRUM mode, cycles STRUMMING->OCTAVE->BANK->STRUMMING
    - test_allowed_modes_keyboard: getAllowedSliderModes returns 4 modes for KEYBOARD
    - test_allowed_modes_quick: getAllowedSliderModes returns 1 mode (QUICK) for QUICK_SETTINGS

    Transport tests:
    - test_mock_transport_receives_note_on: create MockTransport implementing MidiTransport, call sendNoteOn, verify it was received
    - test_mock_transport_receives_note_off: same for NoteOff
    - test_mock_transport_receives_sysex: same for SysEx

    Slider SetPosition tests (FWBUG-02 verification):
    - test_set_position_uint8_assigns_last_position: call SetPosition(3, 7), verify GetPosition() returns 3.0/7.0 (approximately 0.4286)
    - test_set_position_zero_num_positions_no_crash: call SetPosition(0, 0), verify no crash (guard clause), GetPosition() unchanged
    - test_set_position_max_value: call SetPosition(numPositions-1, numPositions), verify GetPosition() close to but less than 1.0
    - test_set_position_float_still_works: call SetPosition(0.5f), verify GetPosition() returns 0.5
  </behavior>
  <action>
1. Update `test/native_stubs/Arduino.h` if needed:
   - Ensure it provides the types needed by ModeManager (or that `src/Types.hpp` can compile in native env)
   - Add any missing Arduino type stubs (uint8_t should already be available via stdint.h)
   - Add stubs for any TouchSlider hardware dependencies (CapTouch, touch_pad_t, etc.) needed to compile TouchSlider.cpp in native env

2. Create `test/test_mode_manager/test_main.cpp`:
   - `#include <unity.h>`
   - `#include "../../src/Types.hpp"` (or path that works with PlatformIO native)
   - Include ModeManager.cpp directly: `#include "../../src/services/ModeManager.cpp"` (following established pattern from test_config_manager)
   - Write the tests listed in <behavior>
   - setUp/tearDown functions (can be empty)
   - `int main()` with `UNITY_BEGIN()`, `RUN_TEST(test_*)`, `UNITY_END()`

3. Create `test/test_transport/test_main.cpp`:
   - `#include <unity.h>`
   - `#include "../../src/Libs/MidiTransport.hpp"`
   - Create a `MockTransport` class implementing `MidiTransport`:
     ```cpp
     class MockTransport : public t16::MidiTransport {
     public:
         int noteOnCount = 0;
         uint8_t lastNote = 0, lastVelocity = 0, lastChannel = 0;
         void sendNoteOn(uint8_t note, uint8_t velocity, uint8_t channel) override {
             noteOnCount++;
             lastNote = note; lastVelocity = velocity; lastChannel = channel;
         }
         // ... implement all pure virtuals similarly
     };
     ```
   - Test that mock properly receives calls and records parameters
   - This validates the interface compiles and can be used for testing

4. Create `test/test_slider/test_main.cpp`:
   - `#include <unity.h>`
   - Include TouchSlider.cpp directly (following established pattern)
   - Stub or mock the hardware dependencies (CapTouch sensor array, touch_pad_read_raw_data, etc.) — only the SetPosition/GetPosition methods need to actually work; hardware init can be stubbed
   - If including all of TouchSlider.cpp pulls in too many hardware dependencies for native compilation, extract just the SetPosition/GetPosition logic:
     - Option A (preferred): Include TouchSlider.cpp with sufficient stubs to compile
     - Option B (fallback): Test the position calculation logic directly by recreating the fixed formula and verifying `lastPosition = static_cast<float>(intPosition) / static_cast<float>(numPositions)`
   - Write the 4 slider tests from <behavior>:
     - `test_set_position_uint8_assigns_last_position`: SetPosition(3, 7) then TEST_ASSERT_FLOAT_WITHIN(0.001, 3.0/7.0, GetPosition())
     - `test_set_position_zero_num_positions_no_crash`: SetPosition(0, 0) does not crash, position unchanged
     - `test_set_position_max_value`: SetPosition(6, 7) -> GetPosition() close to 6.0/7.0
     - `test_set_position_float_still_works`: SetPosition(0.5f) -> GetPosition() == 0.5f
   - These tests serve as the regression gate for FWBUG-02

5. Verify all existing tests still pass alongside new ones.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio test -e native 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f test_mode_manager` passes with 0 failures
    - `pio test -e native -f test_transport` passes with 0 failures
    - `pio test -e native -f test_slider` passes with 0 failures
    - `pio test -e native` (full suite) passes with 0 failures
    - `grep -c "RUN_TEST\|TEST_ASSERT" test/test_mode_manager/test_main.cpp` returns at least 8
    - `grep "MockTransport" test/test_transport/test_main.cpp` returns a match
    - `grep "SetPosition" test/test_slider/test_main.cpp` returns a match
  </acceptance_criteria>
  <done>ModeManager state machine transitions are verified by automated tests. Transport interface is mock-testable. TouchSlider SetPosition bug fix (FWBUG-02) has a regression test. All native tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InputProcessor key-to-note mapping tests</name>
  <files>test/test_input_processor/test_main.cpp</files>
  <read_first>
    - src/services/InputProcessor.hpp (class declaration, method signatures)
    - src/services/InputProcessor.cpp (implementation, especially processKey and note calculation logic)
    - src/Scales.hpp (note_map, scale data — declarations)
    - src/Scales.cpp (scale data — definitions)
    - src/Configuration.hpp (KeyModeData, ConfigurationData, Parameters structs)
    - test/test_mode_manager/test_main.cpp (test pattern from Task 1)
    - test/native_stubs/Arduino.h (available stubs)
  </read_first>
  <behavior>
    - test_key_note_mapping_default_scale: with default scale (chromatic) and base_octave=2, key 0 maps to expected MIDI note
    - test_key_note_mapping_different_octave: changing base_octave changes the output note by 12 per octave
    - test_key_velocity_pressed: when state=PRESSED, velocity is read from keyboard and note-on is sent
    - test_key_released_sends_note_off: when state=RELEASED, note-off is sent for the correct key
    - test_bank_change_updates_scale: calling onBankChange with different bank updates note mapping
  </behavior>
  <action>
1. Create `test/test_input_processor/test_main.cpp`:
   - Include necessary stubs and source files
   - Need to create lightweight mocks for MidiProvider, LedManager, Keyboard:
     - `MockMidiProvider`: records SendNoteOn/SendNoteOff calls (note, velocity, channel)
     - `MockLedManager`: no-op for all LED methods
     - `MockKeyboard`: returns configurable velocity values
   - These mocks can be simple structs defined in the test file
   - Include InputProcessor.cpp and Scales.cpp (for note mapping functions)
   - Include ConfigManager.cpp or create a minimal stub

2. NOTE: InputProcessor takes many dependencies. If including all .cpp files becomes unwieldy for native compilation, simplify:
   - Test the note calculation logic directly (extract the `note = note_map[idx] + base_octave * 12 + 24` formula)
   - Test onBankChange logic with mock config
   - If full InputProcessor instantiation is too complex for native env, test the core logic functions only and document what's not covered

3. Write tests from <behavior> list.

4. Run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio test -e native 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f test_input_processor` passes with 0 failures
    - `pio test -e native` (full suite) passes with 0 failures
    - `grep -c "RUN_TEST\|TEST_ASSERT" test/test_input_processor/test_main.cpp` returns at least 5
    - Tests verify at least: note calculation, octave offset, note-on/note-off dispatch
  </acceptance_criteria>
  <done>InputProcessor key-to-note mapping is verified by automated tests. Core business logic has test coverage. Full native test suite passes.</done>
</task>

</tasks>

<verification>
- `pio test -e native` — all tests pass (including existing config_manager, migration, sysex_protocol tests)
- `pio run -e esp32s3` — firmware still compiles clean
- At least 4 new test suites added (mode_manager, transport, slider, input_processor)
- Tests cover: ModeManager transitions, transport mock dispatch, SetPosition bug fix, key-to-note mapping
</verification>

<success_criteria>
Phase success criteria #5 is met: firmware unit tests run on host covering state machine logic, transport abstraction, slider position fix, and key-to-note mapping. All tests automated via `pio test -e native`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-firmware-service-extraction/02-06-SUMMARY.md`
</output>
