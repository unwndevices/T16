---
phase: 02-firmware-service-extraction
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/services/InputProcessor.hpp
  - src/services/InputProcessor.cpp
  - src/services/SliderProcessor.hpp
  - src/services/SliderProcessor.cpp
  - src/services/ButtonHandler.hpp
  - src/services/ButtonHandler.cpp
  - src/services/CalibrationService.hpp
  - src/services/CalibrationService.cpp
autonomous: true
requirements: [FWARCH-01, FWARCH-04, FWBUG-03, FWBUG-04]

must_haves:
  truths:
    - "ProcessKey, ProcessStrum, ProcessQuickSettings logic lives in InputProcessor, not main.cpp"
    - "ProcessSlider and ProcessSliderButton logic lives in SliderProcessor, not main.cpp"
    - "ProcessButton logic lives in ButtonHandler, not main.cpp"
    - "Unreachable XY_PAD branch (FWBUG-03) is eliminated during extraction"
    - "HardwareTest has a 3-second timeout per key instead of infinite loop (FWBUG-04)"
    - "CalibrationRoutine and HardwareTest live in CalibrationService"
  artifacts:
    - path: "src/services/InputProcessor.hpp"
      provides: "Key/strum/quick-settings event processing"
      contains: "class InputProcessor"
    - path: "src/services/SliderProcessor.hpp"
      provides: "Slider mode processing"
      contains: "class SliderProcessor"
    - path: "src/services/ButtonHandler.hpp"
      provides: "Button event dispatch"
      contains: "class ButtonHandler"
    - path: "src/services/CalibrationService.hpp"
      provides: "Calibration and hardware test"
      contains: "class CalibrationService"
  key_links:
    - from: "src/services/InputProcessor.cpp"
      to: "src/ConfigManager.hpp"
      via: "constructor injection"
      pattern: "ConfigManager&"
    - from: "src/services/ButtonHandler.cpp"
      to: "src/services/ModeManager.hpp"
      via: "constructor injection for mode cycling"
      pattern: "ModeManager&"
    - from: "src/services/SliderProcessor.cpp"
      to: "src/services/ModeManager.hpp"
      via: "queries slider mode"
      pattern: "modeManager_"
---

<objective>
Extract the four remaining service classes from main.cpp: InputProcessor (key/strum/quick-settings), SliderProcessor (slider modes), ButtonHandler (button dispatch), and CalibrationService (hardware test + calibration). Fix FWBUG-03 (dead XY_PAD branch) and FWBUG-04 (hardware test hang) during extraction.

Purpose: These services encapsulate all application logic currently scattered as free functions in main.cpp. After this plan, main.cpp has no business logic — only orchestration glue (which Plan 05 replaces with AppEngine).

Output: Four service classes with dependency injection, two bugs fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-firmware-service-extraction/02-CONTEXT.md
@.planning/phases/02-firmware-service-extraction/02-RESEARCH.md
@.planning/phases/02-firmware-service-extraction/02-01-SUMMARY.md

@src/main.cpp
@src/ConfigManager.hpp
@src/SysExHandler.hpp
@src/services/ModeManager.hpp

<interfaces>
From src/services/ModeManager.hpp (created in Plan 01):
```cpp
namespace t16 {
class ModeManager {
public:
    void setMode(Mode mode);
    Mode getMode() const;
    SliderMode getSliderMode() const;
    void cycleSliderMode();
    SliderMode getDefaultSliderMode(Mode mode) const;
    const SliderMode* getAllowedSliderModes(size_t& count) const;
};
}
```

From src/ConfigManager.hpp:
```cpp
class ConfigManager {
public:
    ConfigurationData& Global();
    KeyModeData& Bank(uint8_t index);
    ControlChangeData& CC(uint8_t index);
    void MarkDirty();
    void CheckIdleFlush(unsigned long now);
    // ...
};
```

From src/Libs/MidiProvider.hpp (public API unchanged from Plan 02):
```cpp
class MidiProvider {
public:
    void SendNoteOn(uint8_t key, uint8_t note, uint8_t velocity, uint8_t channel);
    void SendNoteOff(uint8_t key, uint8_t channel);
    void SendAfterTouch(uint8_t key, uint8_t pressure, uint8_t channel);
    void SendPitchBend(int bend, uint8_t channel);
    void SendControlChange(uint8_t controller, uint8_t value, uint8_t channel);
    void SendChordNoteOn(uint8_t idx, uint8_t note, uint8_t velocity, uint8_t channel);
    void SendChordNoteOff(uint8_t idx, uint8_t channel);
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract InputProcessor service</name>
  <files>src/services/InputProcessor.hpp, src/services/InputProcessor.cpp</files>
  <read_first>
    - src/main.cpp (lines 98-178 for ProcessStrum, ProcessKey, ProcessQuickSettings free functions; lines 68-96 for OnBankChange; lines 543-549 for SetCustomScale; lines 550-583 for ApplyConfiguration)
    - src/services/ModeManager.hpp (created by Plan 01)
    - src/ConfigManager.hpp (API for config access)
    - src/Libs/MidiProvider.hpp (MIDI send methods)
    - src/Libs/Leds/LedManager.hpp (LED control methods after Plan 03 split)
    - src/Libs/Keyboard.hpp (Key::State enum, Keyboard class after Plan 03 split)
    - src/Scales.hpp (SetNoteMap, SetChordMapping declarations after Plan 01 split)
  </read_first>
  <action>
1. Create `src/services/InputProcessor.hpp` with `#pragma once`, inside `namespace t16`:
   - Constructor: `InputProcessor(ConfigManager& config, MidiProvider& midi, LedManager& leds, Keyboard& keyboard)`
   - Public methods:
     - `void processKey(int idx, Key::State state)` — extracted from main.cpp ProcessKey (lines 123-152)
     - `void processStrum(int idx, Key::State state)` — extracted from main.cpp ProcessStrum (lines 102-121)
     - `void processQuickSettings(int idx, Key::State state)` — extracted from main.cpp ProcessQuickSettings (lines 156-178)
     - `void applyConfiguration()` — extracted from main.cpp ApplyConfiguration (lines 552-583)
     - `void onBankChange(uint8_t newBank)` — extracted from main.cpp OnBankChange (lines 78-96)
   - Private members:
     - References: `ConfigManager& configManager_`, `MidiProvider& midiProvider_`, `LedManager& ledManager_`, `Keyboard& keyboard_`
     - Strum state: `uint8_t currentChord_`, `uint8_t currentBaseNote_`, `uint8_t currentKeyIdx_`
     - Quick settings state: `uint8_t currentQsOption_`, `uint8_t currentValueLength_`
   - Per D-02: all dependencies via constructor refs

2. Create `src/services/InputProcessor.cpp`:
   - Move ProcessKey logic: replace `cfg.` with `configManager_.Global().`, `kb_cfg[bank]` with `configManager_.Bank(bank)`, `cc_cfg[bank]` with `configManager_.CC(bank)`, `parameters.bank` with a bank parameter or stored state
   - Move ProcessStrum logic similarly
   - Move ProcessQuickSettings logic similarly
   - Move ApplyConfiguration logic — this applies config to hardware (sensitivity, MIDI settings, brightness, scales). References to `led_manager` become `ledManager_`, `midi_provider` become `midiProvider_`, etc.
   - Move OnBankChange logic — updates palette, note map, chord mapping, keyboard LUTs
   - The `Parameters` struct (runtime state) needs to be accessible. For now, pass a `Parameters&` to the constructor or make it a member. Since Parameters holds runtime state (bank, bend, mod, slew, etc.), it should eventually live in AppEngine. For this task, accept a `Parameters&` reference in the constructor.
   - Add `Parameters& params_` as a private member, add to constructor: `InputProcessor(ConfigManager&, MidiProvider&, LedManager&, Keyboard&, Parameters&)`

3. Do NOT touch main.cpp yet — the free functions remain for now. Plan 05 (AppEngine) will wire InputProcessor and remove the free functions. This task only creates the service class.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class InputProcessor" src/services/InputProcessor.hpp` returns a match
    - `grep -r "processKey" src/services/InputProcessor.cpp` returns a match
    - `grep -r "processStrum" src/services/InputProcessor.cpp` returns a match
    - `grep -r "applyConfiguration" src/services/InputProcessor.cpp` returns a match
    - `grep -r "ConfigManager&" src/services/InputProcessor.hpp` returns a match (DI)
    - `grep -r "namespace t16" src/services/InputProcessor.hpp` returns a match
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>InputProcessor service encapsulates all key/strum/quick-settings processing with dependency injection. No globals referenced — all access through constructor-injected refs.</done>
</task>

<task type="auto">
  <name>Task 2: Extract SliderProcessor, ButtonHandler, and CalibrationService</name>
  <files>src/services/SliderProcessor.hpp, src/services/SliderProcessor.cpp, src/services/ButtonHandler.hpp, src/services/ButtonHandler.cpp, src/services/CalibrationService.hpp, src/services/CalibrationService.cpp</files>
  <read_first>
    - src/main.cpp (lines 180-298 for ProcessSlider; lines 301-339 for ProcessSliderButton; lines 393-541 for ProcessButton; lines 585-685 for HardwareTest and CalibrationRoutine; lines 811-887 for loop() XY_PAD branch at 822-851 and duplicate at 870-873)
    - src/services/ModeManager.hpp (for slider mode queries)
    - src/services/InputProcessor.hpp (created in Task 1 — for cross-reference)
    - src/Libs/TouchSlider.hpp (slider API)
    - src/Libs/Leds/LedManager.hpp (LED API)
    - src/Libs/Button.hpp (Button::State enum)
    - src/Libs/Adc.hpp (for CalibrationService)
  </read_first>
  <action>
**SliderProcessor:**

1. Create `src/services/SliderProcessor.hpp` with `#pragma once`, `namespace t16`:
   - Constructor: `SliderProcessor(ModeManager& modeManager, ConfigManager& config, MidiProvider& midi, LedManager& leds, TouchSlider& slider, Keyboard& keyboard, Parameters& params)`
   - Public: `void update()` — runs the correct slider processing for the current slider mode (extracted from ProcessSlider, lines 180-298)
   - Public: `void onSliderModeChanged()` — configures slider/LEDs when mode changes (extracted from ProcessSliderButton, lines 301-339)
   - Private: references to all injected dependencies
   - Per D-03: tracks own update interval if needed (slider processing may need to run at a specific rate)

2. Create `src/services/SliderProcessor.cpp`:
   - Move ProcessSlider logic: switch on `modeManager_.getSliderMode()` instead of global `slider_mode`
   - Replace all `cfg.` with `configManager_.Global().`, `kb_cfg[]` with `configManager_.Bank()`, etc.
   - Move ProcessSliderButton logic into `onSliderModeChanged()`

**ButtonHandler:**

3. Create `src/services/ButtonHandler.hpp` with `#pragma once`, `namespace t16`:
   - Constructor: `ButtonHandler(ModeManager& modeManager, ConfigManager& config, MidiProvider& midi, InputProcessor& input, SliderProcessor& slider, Parameters& params, Button& touchBtn, Button& modeBtn)`
   - Public: `void handleButton(int idx, Button::State state)` — extracted from ProcessButton (lines 393-541)
   - Note: ButtonHandler needs a reference to InputProcessor to call processQuickSettings and to SliderProcessor to call onSliderModeChanged
   - Private: all injected refs

4. Create `src/services/ButtonHandler.cpp`:
   - Move ProcessButton logic
   - Touch button CLICKED: call `modeManager_.cycleSliderMode()` then `sliderProcessor_.onSliderModeChanged()`
   - Mode button CLICKED: cycle mode via `modeManager_.setMode(nextMode)`, then trigger mode-change effects (call inputProcessor's applyConfiguration or a dedicated onModeChanged method)
   - Both buttons LONG_PRESSED: enter/exit quick settings via ModeManager
   - Mode button LONG_PRESSED alone: MIDI panic via midiProvider
   - Touch button LONG_PRESSED alone: midi learn toggle
   - Per D-04: slider mode cycling uses ModeManager (not inline allowed_modes arrays)

**The loop() mode-specific processing (XY_PAD, KEYBOARD, STRIPS, STRUM draw calls) will be handled in AppEngine (Plan 05). For now, note that the duplicate XY_PAD branch (FWBUG-03, lines 870-873) must NOT be carried into any service — it is dead code and will be dropped.**

**CalibrationService:**

5. Create `src/services/CalibrationService.hpp` with `#pragma once`, `namespace t16`:
   - Constructor: `CalibrationService(Adc& adc, LedManager& leds)`
   - Public: `void runHardwareTest(Key* keys, uint8_t numKeys)` — extracted from HardwareTest (lines 585-608)
   - Public: `void runCalibration(Key* keys, uint8_t numKeys, Button& touchBtn, Button& modeBtn, Keyboard& keyboard, TouchSlider& slider)` — extracted from CalibrationRoutine (lines 610-685)

6. Create `src/services/CalibrationService.cpp`:
   - Move HardwareTest with FWBUG-04 fix per D-07:
     - Replace the infinite `while (!test_passed) { delay(100); }` with a 3-second timeout per key
     - Use `millis()` for timeout: `while (millis() - start < 3000)` per key
     - After all keys tested, if any failed: show red LEDs for failed positions, delay 3 seconds, then continue (don't hang)
     - See 02-RESEARCH.md "HardwareTest with Timeout" code example
   - Move CalibrationRoutine as-is (it already works, just needs to be in its own service)
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class SliderProcessor" src/services/SliderProcessor.hpp` returns a match
    - `grep -r "class ButtonHandler" src/services/ButtonHandler.hpp` returns a match
    - `grep -r "class CalibrationService" src/services/CalibrationService.hpp` returns a match
    - `grep -r "modeManager_" src/services/ButtonHandler.cpp` returns a match (uses ModeManager DI)
    - `grep -r "3000" src/services/CalibrationService.cpp` returns a match (3-second timeout)
    - No `while (!test_passed)` infinite loop in CalibrationService.cpp
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>SliderProcessor, ButtonHandler, and CalibrationService extracted. FWBUG-04 fixed with timeout. FWBUG-03 dead code not carried over. All services use DI.</done>
</task>

</tasks>

<verification>
- `pio run -e esp32s3` compiles clean
- All 4 service .hpp/.cpp pairs exist in src/services/
- No free functions remain that should be in services (ProcessKey, ProcessSlider, ProcessButton, etc. still exist in main.cpp but are shadowed by the new service methods — Plan 05 removes them)
- HardwareTest has timeout, not infinite loop
- Existing test suite passes: `pio test -e native`
</verification>

<success_criteria>
All application logic is encapsulated in service classes. Global state access is replaced with constructor-injected references. Two bugs fixed (FWBUG-03, FWBUG-04). Ready for AppEngine wiring in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/02-firmware-service-extraction/02-04-SUMMARY.md`
</output>
