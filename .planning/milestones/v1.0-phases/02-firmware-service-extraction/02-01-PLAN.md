---
phase: 02-firmware-service-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/ModeManager.hpp
  - src/services/ModeManager.cpp
  - src/Types.hpp
  - src/Scales.cpp
  - src/Scales.hpp
autonomous: true
requirements: [FWARCH-01, FWARCH-03, FWARCH-04]

must_haves:
  truths:
    - "Mode and SliderMode enums are defined in a shared header, not in main.cpp"
    - "ModeManager service owns mode state and provides allowed slider modes per mode"
    - "Scales data and functions are split into .hpp/.cpp, no definitions in the header"
  artifacts:
    - path: "src/services/ModeManager.hpp"
      provides: "ModeManager class declaration"
      contains: "class ModeManager"
    - path: "src/services/ModeManager.cpp"
      provides: "ModeManager implementation with allowed slider modes"
      contains: "getAllowedSliderModes"
    - path: "src/Types.hpp"
      provides: "Shared enums (Mode, SliderMode) used by services and main.cpp"
      contains: "enum class Mode"
    - path: "src/Scales.cpp"
      provides: "Scale data and mapping function implementations"
      contains: "SetNoteMap"
  key_links:
    - from: "src/services/ModeManager.hpp"
      to: "src/Types.hpp"
      via: "includes Mode and SliderMode enums"
      pattern: "#include.*Types\\.hpp"
---

<objective>
Extract shared type definitions (Mode, SliderMode enums) into a common header, create the ModeManager service class, and split Scales.hpp into proper .hpp/.cpp compilation units.

Purpose: ModeManager is the foundational service that other services (InputProcessor, SliderProcessor, ButtonHandler) depend on for mode queries. The shared types header prevents circular dependencies. Scales split prevents multiple-definition linker errors when included from multiple .cpp files.

Output: ModeManager service class, shared Types.hpp, properly split Scales module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-firmware-service-extraction/02-CONTEXT.md
@.planning/phases/02-firmware-service-extraction/02-RESEARCH.md

@src/main.cpp
@src/Configuration.hpp
@src/Scales.hpp
@src/Libs/Types.hpp

<interfaces>
<!-- Existing types from Configuration.hpp that ModeManager will reference -->
From src/Configuration.hpp:
```cpp
struct ConfigurationData {
    uint8_t mode = 0;
    // ...
};
struct Parameters {
    uint8_t bank = 0;
    // ...
};
```

From src/main.cpp (lines 56-66) — enums to extract:
```cpp
enum SliderMode { BEND, OCTAVE, MOD, BANK, SLEW, STRUMMING, QUICK, SLIDER_MODE_AMOUNT };
```

From src/main.cpp (Mode is referenced as cfg.mode with values like Mode::KEYBOARD, Mode::XY_PAD, etc.)
Mode enum is currently defined in Keyboard.hpp or Types.hpp.

From src/Libs/Types.hpp:
```cpp
// Contains Vec2 and possibly Mode enum
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Types.hpp and ModeManager service</name>
  <files>src/Types.hpp, src/services/ModeManager.hpp, src/services/ModeManager.cpp</files>
  <read_first>
    - src/Libs/Types.hpp (current type definitions)
    - src/main.cpp (lines 56-66 for SliderMode enum; lines 341-391 for ProcessModeButton with mode-to-slider mapping; lines 393-453 for ProcessButton slider mode cycling)
    - src/Libs/Keyboard.hpp (for Mode enum definition location)
    - src/Configuration.hpp (for struct definitions used by services)
  </read_first>
  <action>
1. Create `src/Types.hpp` (replacing `src/Libs/Types.hpp`) with `#pragma once`. Move into `namespace t16`:
   - `Mode` enum (currently in Keyboard.hpp or Types.hpp): `KEYBOARD, XY_PAD, STRIPS, STRUM, QUICK_SETTINGS, MODE_AMOUNT`
   - `SliderMode` enum (currently in main.cpp lines 56-66): `BEND, OCTAVE, MOD, BANK, SLEW, STRUMMING, QUICK, SLIDER_MODE_AMOUNT`
   - Keep `Vec2` struct if it exists in current Types.hpp
   - Use `enum class` for both (scoped enums) -- BUT check if existing code uses unscoped values like `Mode::KEYBOARD` (if so, keep unscoped for now to avoid breaking changes across the entire codebase; migration to enum class can happen later)

2. Create `src/services/` directory.

3. Create `src/services/ModeManager.hpp` with `#pragma once`, inside `namespace t16`:
   - Private member: `Mode currentMode_` initialized to `Mode::KEYBOARD`
   - Private member: `SliderMode currentSliderMode_` initialized to `SliderMode::BEND`
   - Public: `void setMode(Mode mode)` — sets currentMode_ and resets slider mode to the default for that mode
   - Public: `Mode getMode() const`
   - Public: `SliderMode getSliderMode() const`
   - Public: `void cycleSliderMode()` — advances to next allowed slider mode for current mode (per D-04)
   - Public: `SliderMode getDefaultSliderMode(Mode mode) const` — returns the default slider mode per mode
   - Public: `const SliderMode* getAllowedSliderModes(size_t& count) const` — returns pointer to static array of allowed modes for current mode
   - Constructor: default
   - Follow trailing underscore convention for private members (per D-02, eisei convention)

4. Create `src/services/ModeManager.cpp`:
   - Implement `getAllowedSliderModes` using static constexpr arrays per mode (from 02-RESEARCH.md Pattern 4):
     - KEYBOARD: {BEND, MOD, OCTAVE, BANK}
     - STRUM: {STRUMMING, OCTAVE, BANK}
     - XY_PAD, STRIPS: {SLEW, BANK}
     - QUICK_SETTINGS: {QUICK}
   - Implement `cycleSliderMode()`: find current in allowed list, advance by 1, wrap around
   - Implement `setMode()`: set currentMode_, set currentSliderMode_ to getDefaultSliderMode(mode)
   - Implement `getDefaultSliderMode()`: first element of each mode's allowed list

5. Update `src/Libs/Types.hpp` to just `#include "../Types.hpp"` as a redirect so existing includes don't break.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class ModeManager" src/services/ModeManager.hpp` returns a match
    - `grep -r "getAllowedSliderModes" src/services/ModeManager.cpp` returns a match
    - `grep -r "enum.*SliderMode" src/Types.hpp` returns a match (SliderMode in shared header)
    - `grep -r "namespace t16" src/services/ModeManager.hpp` returns a match
    - `grep -r "currentMode_" src/services/ModeManager.hpp` returns a match (trailing underscore)
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>ModeManager service exists with mode state, allowed slider modes, and cycle logic. Shared Types.hpp contains Mode and SliderMode enums. Firmware compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Split Scales.hpp into .hpp/.cpp</name>
  <files>src/Scales.hpp, src/Scales.cpp</files>
  <read_first>
    - src/Scales.hpp (full file — need to identify all data arrays, free functions, and what must move to .cpp)
    - src/main.cpp (grep for SetNoteMap, SetChordMapping, SetCustomScale, note_map, scales, strum_chords, current_chord_mapping usage)
  </read_first>
  <action>
1. Read `src/Scales.hpp` to identify all:
   - Data arrays defined at file scope (e.g., `scales[]`, `note_map[]`, `strum_chords[]`, `current_chord_mapping[]`)
   - Free functions (e.g., `SetNoteMap()`, `SetChordMapping()`)

2. Modify `src/Scales.hpp`:
   - Keep `#pragma once` (replace `#ifndef` guard if present)
   - Keep type definitions, constants, and function declarations
   - Replace array definitions with `extern` declarations
   - Replace function definitions with function declarations only
   - Keep callback typedef/using if present

3. Create `src/Scales.cpp`:
   - `#include "Scales.hpp"`
   - Move all array definitions here (e.g., `int8_t scales[][16] = { ... }`)
   - Move all function implementations here (e.g., `void SetNoteMap(...) { ... }`)
   - Move `note_map[]`, `strum_chords[]`, `current_chord_mapping[]` definitions here

4. Verify no data is defined in the header (only declared with `extern`).
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^extern" src/Scales.hpp` returns at least 1 (extern declarations present)
    - `grep -c "SetNoteMap" src/Scales.cpp` returns at least 1 (implementation moved to .cpp)
    - No array initializers remain in src/Scales.hpp (grep for `= {` should return 0 or only small inline constexpr)
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>Scales module properly split: header has declarations only, .cpp has all data and implementations. No multiple-definition linker errors possible.</done>
</task>

</tasks>

<verification>
- `pio run -e esp32s3` compiles clean
- ModeManager.hpp and ModeManager.cpp exist in src/services/
- Types.hpp contains both Mode and SliderMode enums
- Scales.hpp has no data definitions (only extern declarations)
- Existing test suite still passes: `pio test -e native`
</verification>

<success_criteria>
ModeManager service is ready for consumption by InputProcessor, SliderProcessor, and ButtonHandler (Plan 04). Shared type header eliminates enum duplication. Scales module is safe for multi-TU inclusion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-firmware-service-extraction/02-01-SUMMARY.md`
</output>
