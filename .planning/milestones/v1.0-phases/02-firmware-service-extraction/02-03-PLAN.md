---
phase: 02-firmware-service-extraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Libs/Keyboard.hpp
  - src/Libs/Keyboard.cpp
  - src/Libs/Button.hpp
  - src/Libs/Button.cpp
  - src/Libs/Leds/LedManager.hpp
  - src/Libs/Leds/LedManager.cpp
  - src/Libs/Leds/patterns/Pattern.hpp
  - src/Libs/Leds/patterns/Pattern.cpp
autonomous: true
requirements: [FWARCH-03, FWBUG-01]

must_haves:
  truths:
    - "Keyboard.hpp has no function or static member definitions — all in Keyboard.cpp"
    - "Button.hpp has no function definitions — all in Button.cpp"
    - "LedManager uses std::unique_ptr for pattern ownership — no raw new/delete"
    - "Pattern static members are defined in Pattern.cpp, not Pattern.hpp"
    - "LED global arrays are defined in LedManager.cpp, not the header"
  artifacts:
    - path: "src/Libs/Keyboard.cpp"
      provides: "Keyboard and Key implementation"
      contains: "Key::Update"
    - path: "src/Libs/Button.cpp"
      provides: "Button implementation"
      contains: "Button::Update"
    - path: "src/Libs/Leds/LedManager.cpp"
      provides: "LedManager implementation with unique_ptr patterns"
      contains: "unique_ptr"
    - path: "src/Libs/Leds/patterns/Pattern.cpp"
      provides: "Pattern static member definitions"
      contains: "Pattern::"
  key_links:
    - from: "src/Libs/Leds/LedManager.cpp"
      to: "src/Libs/Leds/patterns/Pattern.hpp"
      via: "includes Pattern and uses make_unique for transitions"
      pattern: "make_unique"
---

<objective>
Split header-only implementations (Keyboard, Button, LedManager, Pattern) into proper .hpp/.cpp pairs, and fix the LedManager memory leak by replacing raw Pattern pointers with std::unique_ptr.

Purpose: Header-only files with static member definitions and global variables cause multiple-definition linker errors when included from multiple .cpp files (which will happen after service extraction). The LedManager leak (FWBUG-01) loses memory on every mode switch.

Output: Properly split compilation units, leak-free pattern ownership.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-firmware-service-extraction/02-CONTEXT.md
@.planning/phases/02-firmware-service-extraction/02-RESEARCH.md

@src/Libs/Keyboard.hpp
@src/Libs/Button.hpp
@src/Libs/Leds/LedManager.hpp
@src/Libs/Leds/patterns/Pattern.hpp

<interfaces>
From 02-RESEARCH.md Header-Only Extraction Audit:
- Keyboard.hpp (513 lines): 4 static members (press_threshold, rel_threshold, at_threshold, instances), free function fmap()
- Button.hpp (191 lines): No statics, no free functions
- LedManager.hpp (312 lines): Globals (leds_plus_safety_pixel, matrixleds, sliderleds, patternleds), free function XY()
- Pattern.hpp (121 lines): 4 statics (currentPalette, targetPalette, selectedOption, etc.), free functions wu_pixel(), wu_pixel_1d()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split Keyboard.hpp and Button.hpp into .hpp/.cpp pairs</name>
  <files>src/Libs/Keyboard.hpp, src/Libs/Keyboard.cpp, src/Libs/Button.hpp, src/Libs/Button.cpp</files>
  <read_first>
    - src/Libs/Keyboard.hpp (full file — identify all method implementations, static member definitions, free functions like fmap())
    - src/Libs/Button.hpp (full file — identify all method implementations)
    - src/main.cpp (lines 36-43 for Key/Keyboard usage, grep for keyboard. and keys[ usage patterns)
  </read_first>
  <action>
**Keyboard split:**

1. Modify `src/Libs/Keyboard.hpp`:
   - Replace `#ifndef`/`#define` guard with `#pragma once`
   - Keep all class declarations (Key, KeyboardConfig, Keyboard) with method signatures only
   - Keep enum definitions (Key::State, Keyboard::Lut) inline in the class
   - Remove all method bodies — replace with declarations only (`;` instead of `{ ... }`)
   - Remove static member DEFINITIONS at file scope (e.g., `float Key::press_threshold = 0.15f;`) — replace with `static float press_threshold;` in the class declaration (if not already)
   - Remove the `fmap()` free function body — declare it: `float fmap(float x, float in_min, float in_max, float out_min, float out_max);`
   - Keep `#include` directives, keep `Key::State` and `Keyboard::Lut` enum definitions

2. Create `src/Libs/Keyboard.cpp`:
   - `#include "Keyboard.hpp"` and any other needed includes
   - Move ALL method implementations here (Key::Update, Key::Init, Keyboard::Init, Keyboard::Update, Keyboard::SetOnStateChanged, etc.)
   - Move static member definitions here: `float Key::press_threshold = 0.15f;` (use actual values from the header)
   - Move `fmap()` implementation here
   - Move the `instances` static member definition here
   - Move any LUT data arrays (velocity curves, aftertouch curves) here

**Button split:**

3. Modify `src/Libs/Button.hpp`:
   - Replace `#ifndef`/`#define` guard with `#pragma once`
   - Keep class declaration with method signatures only
   - Keep enum definitions (Button::State) inline
   - Remove all method bodies

4. Create `src/Libs/Button.cpp`:
   - `#include "Button.hpp"`
   - Move ALL method implementations here (Button::Init, Button::Update, etc.)
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/Libs/Keyboard.cpp` exists and `grep -c "Key::" src/Libs/Keyboard.cpp` returns at least 5
    - `src/Libs/Button.cpp` exists and `grep -c "Button::" src/Libs/Button.cpp` returns at least 3
    - `grep -c "^{" src/Libs/Keyboard.hpp` returns 0 or near-0 (no function bodies in header, only enum braces and class braces)
    - `grep "pragma once" src/Libs/Keyboard.hpp` returns a match
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>Keyboard and Button are properly split .hpp/.cpp pairs. Static members defined in .cpp only. Safe for multi-TU inclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Split LedManager and Pattern, fix memory leak with unique_ptr (FWBUG-01)</name>
  <files>src/Libs/Leds/LedManager.hpp, src/Libs/Leds/LedManager.cpp, src/Libs/Leds/patterns/Pattern.hpp, src/Libs/Leds/patterns/Pattern.cpp</files>
  <read_first>
    - src/Libs/Leds/LedManager.hpp (full file — identify globals like leds_plus_safety_pixel, matrixleds, sliderleds, patternleds, XY() function, all method bodies, TransitionToPattern signature)
    - src/Libs/Leds/patterns/Pattern.hpp (full file — identify static members, wu_pixel functions)
    - src/main.cpp (lines 24-31 for pattern instances touch_blur/no_blur/strips/strum/quick; lines 341-391 for TransitionToPattern calls with raw pointers like &no_blur)
    - src/Libs/Leds/patterns/WaveTransition.hpp (understand transition pattern — RunPattern returns bool, transition completion logic)
    - src/Libs/Leds/patterns/TouchBlur.hpp (representative pattern implementation)
  </read_first>
  <action>
**Pattern.hpp split:**

1. Modify `src/Libs/Leds/patterns/Pattern.hpp`:
   - Replace include guard with `#pragma once`
   - Keep class declaration with virtual method signatures
   - REMOVE static member definitions at file scope (e.g., `CRGBPalette16 Pattern::currentPalette = ...;`)
   - Keep `static CRGBPalette16 currentPalette;` DECLARATION in the class
   - Remove `wu_pixel()` and `wu_pixel_1d()` function bodies — declare only
   - Keep all includes needed by pattern subclasses

2. Create `src/Libs/Leds/patterns/Pattern.cpp`:
   - `#include "Pattern.hpp"`
   - Move static member definitions here
   - Move `wu_pixel()` and `wu_pixel_1d()` implementations here

**LedManager split + unique_ptr fix (D-06, FWBUG-01):**

3. Modify `src/Libs/Leds/LedManager.hpp`:
   - Replace include guard with `#pragma once`
   - Add `#include <memory>` for unique_ptr
   - Remove global variable DEFINITIONS (`CRGB leds_plus_safety_pixel[...]`, `CRGB* matrixleds`, `CRGB* sliderleds`, etc.) — use `extern` declarations or make them private members of LedManager
   - Make LED arrays private members of LedManager:
     ```cpp
     CRGB leds_[NUM_LEDS + 1];  // safety pixel included
     CRGB* matrixleds_;         // points into leds_
     CRGB* sliderleds_;         // points into leds_
     ```
   - Replace `Pattern* currentPattern` and `Pattern* nextPattern` with:
     ```cpp
     std::unique_ptr<Pattern> currentPattern_;
     std::unique_ptr<Pattern> nextPattern_;
     ```
   - Change `TransitionToPattern` signature from `void TransitionToPattern(Pattern* p)` to `void transitionToPattern(std::unique_ptr<Pattern> pattern)` per D-06
   - Remove `XY()` function body — declare only
   - Remove all method bodies — declarations only

4. Create `src/Libs/Leds/LedManager.cpp`:
   - `#include "LedManager.hpp"`
   - `#include <FastLED.h>` and pattern includes
   - Move all method implementations
   - Move `XY()` implementation
   - Initialize LED array pointers in constructor or Init()
   - Implement `transitionToPattern(std::unique_ptr<Pattern>)`:
     ```cpp
     void LedManager::transitionToPattern(std::unique_ptr<Pattern> pattern) {
         nextPattern_ = std::move(pattern);
         currentPattern_ = std::make_unique<WaveTransition>();
     }
     ```
   - In RunPattern or equivalent, when transition completes:
     ```cpp
     currentPattern_ = std::move(nextPattern_);
     ```

5. The global pattern instances in main.cpp (`touch_blur`, `no_blur`, `strips`, `strum`, `quick` on lines 27-31) are stack-allocated. With unique_ptr, callers will need to pass `std::make_unique<NoBlur>()` instead of `&no_blur`. For now, keep the old instances in main.cpp but update the TransitionToPattern calls to use make_unique. The old globals become unused and can be removed. Since main.cpp will be rewritten in Plan 05 (AppEngine), just ensure LedManager's API compiles. Update the TransitionToPattern calls in ProcessModeButton (main.cpp lines 341-391) from `TransitionToPattern(&no_blur)` to `transitionToPattern(std::make_unique<NoBlur>())`.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16 && pio run -e esp32s3 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/Libs/Leds/LedManager.cpp` exists and `grep -c "LedManager::" src/Libs/Leds/LedManager.cpp` returns at least 5
    - `src/Libs/Leds/patterns/Pattern.cpp` exists and `grep -c "Pattern::" src/Libs/Leds/patterns/Pattern.cpp` returns at least 1
    - `grep "unique_ptr" src/Libs/Leds/LedManager.hpp` returns a match
    - `grep "make_unique" src/Libs/Leds/LedManager.cpp` returns a match
    - No raw `new WaveTransition` or `new Pattern` in LedManager (grep should return 0)
    - `grep "pragma once" src/Libs/Leds/LedManager.hpp` returns a match
    - `pio run -e esp32s3` compiles without errors
  </acceptance_criteria>
  <done>LedManager and Pattern properly split. Memory leak fixed with unique_ptr pattern ownership. LED globals are private members. FWBUG-01 resolved.</done>
</task>

</tasks>

<verification>
- `pio run -e esp32s3` compiles clean
- All 4 new .cpp files exist (Keyboard, Button, LedManager, Pattern)
- No static member definitions remain in any .hpp header (only declarations)
- LedManager uses unique_ptr (no raw new/delete for patterns)
- Existing test suite passes: `pio test -e native`
</verification>

<success_criteria>
All header-only implementations are split into proper compilation units. No multiple-definition linker errors when headers are included from multiple .cpp files. LedManager pattern transitions are memory-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/02-firmware-service-extraction/02-03-SUMMARY.md`
</output>
