---
phase: 09-ui-wiring-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - editor-tx/src/components/NavBar/NavBar.tsx
  - editor-tx/src/components/NavBar/NavBar.module.css
  - editor-tx/src/pages/Dashboard/Dashboard.tsx
autonomous: true
requirements: [WEBFEAT-02]
gap_closure: true

must_haves:
  truths:
    - "User can initiate a BLE connection from the NavBar"
    - "User can initiate a BLE connection from the Dashboard empty state"
    - "Calibration and factory reset commands are sent over BLE when connected via BLE"
  artifacts:
    - path: "editor-tx/src/components/NavBar/NavBar.tsx"
      provides: "BLE connect option in NavBar actions"
      contains: "connectBLE"
    - path: "editor-tx/src/pages/Dashboard/Dashboard.tsx"
      provides: "BLE connect option in EmptyState, transport-agnostic calibration/reset"
      contains: "connectBLE"
  key_links:
    - from: "editor-tx/src/components/NavBar/NavBar.tsx"
      to: "ConnectionContext.connectBLE"
      via: "useConnection() hook"
      pattern: "connectBLE"
    - from: "editor-tx/src/pages/Dashboard/Dashboard.tsx"
      to: "ConnectionContext.transport"
      via: "useConnection() hook"
      pattern: "transport.*output"
---

<objective>
Wire the BLE connect action into NavBar and Dashboard EmptyState, and fix calibration/factory reset to use transport-agnostic sender so all device features work over both USB and BLE.

Purpose: Close the two BLE-related gaps from milestone audit -- BLE connect is unreachable and calibration/reset bypasses BLE transport.
Output: NavBar and EmptyState offer BLE connect option; SettingsTab sends commands via transport ?? output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From editor-tx/src/types/midi.ts:
```typescript
export interface ConnectionContextValue extends ConnectionState {
  connect(): Promise<void>
  connectBLE(): Promise<void>
  disconnect(): void
  setDemo(demo: boolean): void
}
```

From editor-tx/src/hooks/useConnection.ts:
```typescript
export function useConnection(): ConnectionContextValue
```

From editor-tx/src/services/midi.ts:
```typescript
export function requestCalibration(sender: SysExSender): void
export function requestFactoryReset(sender: SysExSender): void
```

From editor-tx/src/protocol/sysex.ts:
```typescript
export type SysExSender = Output | MidiTransport
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BLE connect option to NavBar and Dashboard EmptyState</name>
  <files>
    editor-tx/src/components/NavBar/NavBar.tsx,
    editor-tx/src/components/NavBar/NavBar.module.css,
    editor-tx/src/pages/Dashboard/Dashboard.tsx
  </files>
  <read_first>
    editor-tx/src/components/NavBar/NavBar.tsx,
    editor-tx/src/components/NavBar/NavBar.module.css,
    editor-tx/src/pages/Dashboard/Dashboard.tsx,
    editor-tx/src/hooks/useConnection.ts,
    editor-tx/src/types/midi.ts
  </read_first>
  <action>
    In NavBar.tsx:
    1. Destructure `connectBLE` from `useConnection()` alongside existing `isConnected`, `connect`, `disconnect`.
    2. When NOT connected, replace the single "Connect Device" button with TWO buttons side by side:
       - "USB" button (variant="primary", size="sm") calling `connect`
       - "BLE" button (variant="secondary", size="sm") calling `connectBLE`
    3. When connected, keep the existing single "Disconnect" button unchanged.
    4. Import `MdBluetooth` from `react-icons/md` and `MdUsb` from `react-icons/md` for the button icons.
    5. Each button should show only the icon + short label: icon + "USB", icon + "BLE".

    In NavBar.module.css:
    6. Add a `.connectGroup` class for the two-button layout:
       ```css
       .connectGroup {
         display: flex;
         gap: var(--space-xs);
       }
       ```

    In Dashboard.tsx EmptyState component:
    7. Add `onConnectBLE` prop (type `() => void`) to EmptyState.
    8. Render two buttons in EmptyState instead of one:
       - "Connect USB" (variant="primary") calling `onConnect`
       - "Connect BLE" (variant="secondary") calling `onConnectBLE`
    9. In the `Dashboard` component, destructure `connectBLE` from `useConnection()` and pass it to EmptyState:
       `<EmptyState onConnect={connect} onConnectBLE={connectBLE} />`
    10. Update the EmptyState body text: "Connect your T16 via USB or Bluetooth to start editing."
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16/editor-tx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - NavBar.tsx contains `connectBLE` destructured from useConnection
    - NavBar.tsx renders "USB" and "BLE" buttons when not connected
    - Dashboard.tsx EmptyState accepts and uses `onConnectBLE` prop
    - Dashboard.tsx passes `connectBLE` to EmptyState
    - grep -q "connectBLE" editor-tx/src/components/NavBar/NavBar.tsx returns 0
    - grep -q "connectBLE" editor-tx/src/pages/Dashboard/Dashboard.tsx returns 0
    - grep -q "connectGroup" editor-tx/src/components/NavBar/NavBar.module.css returns 0
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>NavBar shows USB and BLE connect buttons when disconnected. EmptyState shows both connection options. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix calibration/factory reset to use transport-agnostic sender</name>
  <files>
    editor-tx/src/pages/Dashboard/Dashboard.tsx
  </files>
  <read_first>
    editor-tx/src/pages/Dashboard/Dashboard.tsx,
    editor-tx/src/types/midi.ts,
    editor-tx/src/protocol/sysex.ts
  </read_first>
  <action>
    In Dashboard.tsx SettingsTab function:
    1. Change line 281 from `const { output } = useConnection()` to `const { output, transport } = useConnection()`.
    2. Change line 368 from `if (output) requestCalibration(output)` to:
       ```typescript
       const sender = transport ?? output
       if (sender) requestCalibration(sender)
       ```
    3. Change line 395 from `if (output) requestFactoryReset(output)` to:
       ```typescript
       const sender = transport ?? output
       if (sender) requestFactoryReset(sender)
       ```
    4. To avoid repeating `transport ?? output`, define a local `sender` const at the top of the component body:
       ```typescript
       const { output, transport } = useConnection()
       const sender = transport ?? output
       ```
       Then use `sender` in both onClick handlers: `if (sender) requestCalibration(sender)` and `if (sender) requestFactoryReset(sender)`.
  </action>
  <verify>
    <automated>cd /home/unwn/git/T16/editor-tx && npx tsc --noEmit 2>&1 | head -20 && grep -n "transport.*output" src/pages/Dashboard/Dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsTab destructures `transport` from useConnection
    - SettingsTab defines `const sender = transport ?? output`
    - requestCalibration is called with `sender` not `output`
    - requestFactoryReset is called with `sender` not `output`
    - grep -q "transport ?? output" editor-tx/src/pages/Dashboard/Dashboard.tsx returns 0
    - No direct `requestCalibration(output)` or `requestFactoryReset(output)` remains
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Calibration and factory reset commands use transport-agnostic sender (transport ?? output), working over both USB and BLE connections.</done>
</task>

</tasks>

<verification>
1. `cd /home/unwn/git/T16/editor-tx && npx tsc --noEmit` -- zero TypeScript errors
2. `grep -c "connectBLE" editor-tx/src/components/NavBar/NavBar.tsx` -- at least 1 match
3. `grep -c "connectBLE" editor-tx/src/pages/Dashboard/Dashboard.tsx` -- at least 1 match
4. `grep "transport.*output" editor-tx/src/pages/Dashboard/Dashboard.tsx` -- shows transport ?? output pattern
5. No remaining `requestCalibration(output)` or `requestFactoryReset(output)` patterns
</verification>

<success_criteria>
- BLE connect is callable from both NavBar and Dashboard EmptyState
- Calibration and factory reset work over BLE (transport-agnostic sender)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-wiring-gap-closure/09-01-SUMMARY.md`
</output>
